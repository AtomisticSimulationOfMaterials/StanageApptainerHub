# ----------------------------------------------------------
# Thermalization Script for BCC Iron - Kokkos
# Author: Ethan L. Edmunds
# Year: 2025
# Purpose: Equilibrate a BCC Iron system using NPT dynamics
# ----------------------------------------------------------

# ---------- Initialize Simulation ------------------------
clear
units metal
dimension 3
boundary p p p
atom_style atomic
atom_modify map array

# ---------- Create Atomistic System ----------------------
lattice bcc 2.866
region box block 0 40 0 40 0 40
create_box 1 box
create_atoms 1 box

# ---------- Define Interatomic Potential -----------------
pair_style eam/fs
pair_coeff * * ../00_potentials/malerba.fs Fe

# Neighbor list settings
neighbor 2.0 bin
neigh_modify delay 10 check yes

# ---------- Set Simulation Parameters -------------------
reset_timestep 0
timestep 0.001

# Assign initial velocities
velocity all create 1000.0 124312 mom yes rot yes dist gaussian

# Apply NPT ensemble for thermalization
fix 1 all npt temp 1000.0 1000.0 $(100.0*dt) iso 0.0 0.0 $(100.0*dt)

# ---------- Output Settings -----------------------------
thermo 10
thermo_style custom step dt time temp press ke pe etotal

# ---------- Run Simulation -------------------------------
run 500
